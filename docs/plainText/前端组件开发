### 前端开发需求（基于Vant4组件库）
1. 技术基础：
   - 项目包管理器为pnpm，已通过`pnpm add vant`安装Vant4组件库；
   - 需基于Vant4官方文档实现开发，优先遵循文档中的最佳实践。

2. 核心要求：
   - 组件引入方式：严格采用「按需引入」（非全量引入），需给出具体的配置步骤/代码示例（如结合unplugin-vue-components自动按需引入，或手动按需引入）；
   - 样式规范：保证Vant组件样式的统一性，包括：
     - 全局统一配置Vant主题色、字体、圆角等基础样式；
     - 自定义组件样式时避免覆盖Vant默认样式导致的视觉不一致；
     - 不同页面中同类型Vant组件（如按钮、输入框）的样式/尺寸保持统一。
   - 组件封装：对高频使用的Vant组件（如表单输入项、列表项、弹窗等）进行二次封装，要求：
     - 封装后的组件具备可复用性、可配置性（通过props传参）；
     - 封装逻辑清晰，符合Vue组件设计规范；
     - 给出具体的封装示例（如封装一个带校验的Vant输入框组件）。

3. 辅助要求：
   - 需标注Vant4的版本特性（如与Vant3的差异点），避免使用废弃API；
   - 所有示例代码需适配Vue3 + vite（如组合式API setup语法）。
# 功能迭代记录

> 按时间顺序记录每次功能迭代的变更。

---

## 2026-06-16 · 菜单实体重构 + 管理端通用组件 + Loading 体系（5 commits, +3943/-1399）

**后端**
- `Menu` 实体重构：新增 `path`、`component`、`query`、`isExt`、`isCache`、`isHidden` 等字段，支持前端动态路由生成
- `MenuServiceImpl` 菜单查询逻辑重写，支持树形路由返回
- `AsyncRouteVO` 扩展 `isHidden` 字段
- 新增 `JsonTypeHandler` 通用 JSON 类型处理器
- `AuthTokenVO` 新增 `tenantId` 字段
- 菜单 SQL 迁移脚本：新增日记/打卡/记账/纪念日等业务菜单

**小程序端**
- 新增全局 `app-loading` 组件 + `useLoading` composable 统一加载态管理
- 打卡页重构：Hero 进度区 + 任务列表优化 + FAB 子菜单定位修复
- 纪念日页面 UI 重构 + Hero 高度缩减
- 记账年度图表、日记详情等页面微调

**管理端（admin）**
- 路由改为动态生成（后端菜单驱动），移除硬编码路由模块
- 新增 10+ 通用管理组件：`DictTag`、`FileUpload`、`ImageUpload`、`ImagePreview`、`Pagination`、`RightToolbar`、`TreeSelect`、`Iframe` 等
- 打卡/日记/记账/纪念日 4 个业务管理页面完整实现（列表 + 增删改查）
- 全局组件类型声明 `global-components.d.ts`

**文档清理**
- 移除 `docs/changelog/` 旧文档

---

## 2026-06-15 · 打卡模块 V2 全量升级 + UI 重构（18 commits）

**打卡 V2 数据库**
- 新增 `biz_checkin_tag` 标签表 + 20 个预设标签、`biz_checkin_record_tag` 关联表
- 新增 `biz_achievement_medal` 勋章表 + 12 个预设、`biz_user_medal` 用户勋章表
- 新增 `biz_checkin_media` 附件表
- `biz_checkin_record` 新增 `mood`、`is_mend` 字段

**打卡 V2 后端**
- 热力图接口（按月统计 + 连续天数）、标签 CRUD、补卡接口、勋章列表 + 自动解锁
- `CheckinTaskVO` 扩展 mood/tagNames/remark/mediaPaths

**打卡 V2 前端**
- 热力图日历组件、心情选择器、标签多选器、勋章卡片 + 解锁动效
- 历史记录拆分为独立页面，支持日期跳转
- 打卡弹窗集成心情/标签/备注/图片

**全站 UI 风格统一（7 个页面）**
- 个人中心、编辑资料、提醒设置、回收站、去年今日、账本管理、记账主页
- 统一 Hero 渐变头部 + 卡片内容区，功能色：日记蓝、记账绿、打卡紫、回忆橙

**日记 + 记账 UI 重构**
- 日记列表/详情/编辑器 UI 重构
- 记账弹窗/底部操作栏/年度图表/分类图表（环形图）/统计面板吸顶优化

**Bug 修复**
- `CheckinRecordTag` 继承 `BaseEntity` 查询报错、热力图 `java.sql.Date` 类型异常
- 统计面板穿透弹窗、FAB 子菜单定位、URLSearchParams 拼接问题

**文档整理**
- `PROJECT_SUMMARY.md` → `docs/项目总结.md`，打卡设计文档合并
- 新增迭代记录、README 重构、提交规范补充

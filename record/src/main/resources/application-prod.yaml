# 生产环境配置。
# 生产环境不要在仓库内保留真实密钥，统一通过环境变量注入。

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://123.207.210.150:3306/life_record?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:wyh20040118}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DATABASE:0}
      timeout: 3s
  ai:
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: ${AI_CHAT_MEMORY_INIT_SCHEMA:never}

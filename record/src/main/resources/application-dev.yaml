# 开发环境配置。
# 本地调试时可以直接依赖默认值启动，敏感信息优先由环境变量覆盖。

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://123.207.210.150:3306/life_record?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: root
    password: ${DB_PASSWORD:wyh20040118}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 123.207.210.150
      port: 6379
      password: wyh20040118
      database: 0
      timeout: 10s
  ai:
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
    # vectorstore.milvus 不在 dev 覆盖，自动用 application.yaml 默认值
    # → host=${MILVUS_HOST:127.0.0.1} port=${MILVUS_PORT:19530}
    # 本地起 docker-compose.dev.yml 里的 milvus-standalone 就能连

logging:
  level:
    org.apache.ibatis: warn
    org.mybatis: warn
    org.mybatis.spring: warn
    com.record.modules.tag.mapper: debug

# 2026-06-28 工作日志

## CodeMirror 6 编辑器完善

### 修复 tags.code 崩溃
- `@lezer/highlight` 没有 `tags.code`，改为用 `tags.monospace`
- 文件：`admin/src/composables/markdown-editor/theme.ts`

### 工具栏功能补充
- `MarkdownEditor.vue` 添加了完整工具栏：加粗、斜体、删除线、标题、链接、图片、行内代码、代码块、引用、列表、分割线
- 代码块支持语言选择（popover 选择器，支持 JS/TS/Python/Java/Go/Rust/SQL/JSON/YAML/HTML/CSS/Shell/Docker/XML）
- 补充表格插入（`insertTable`）和任务列表（`insertTaskItem`）

### 左右分屏模式
- `editor.vue` 添加三模式切换：`edit`（只编辑）→ `split`（左编辑右预览）→ `preview`（只预览）
- 分屏模式下 `editor-wrapper` 取消 `max-width` 限制
- 文件：`admin/src/views/business/blog/editor.vue`

## 技术要点
- CM6 编辑器通过 `view.dispatch({ changes })` 插入内容
- 分屏模式用 `v-if/v-else-if/v-else` 切换，不是 `v-show`（避免编辑器实例冲突）
- Element Plus `ElPopover` 用于代码块语言选择

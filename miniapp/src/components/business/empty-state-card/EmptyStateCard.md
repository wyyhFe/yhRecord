# 空状态卡片
用于列表页、管理页和占位页的统一空状态展示组件。

## 适用场景
- 列表暂无数据
- 筛选结果为空
- 后端暂未配置模板
- 功能页暂时没有内容

## 设计说明
- 内部基于 `uView Pro` 的 `u-empty`
- 外层统一使用项目里的 `section-shell` 容器样式
- 支持标题、描述和底部操作插槽

## 常用属性
- `title`：空状态主文案
- `description`：补充说明
- `mode`：`u-empty` 的展示模式，默认 `data`

## 插槽
- `action`：底部操作区，比如“立即新建”“刷新列表”
